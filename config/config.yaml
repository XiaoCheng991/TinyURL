# TinyURL 配置文件

# 应用配置
app:
  host: "0.0.0.0"
  port: 8088

# 数据库配置 (MySQL)
database:
  host: "localhost"
  port: 3306
  username: "root"
  password: "admin"
  name: "tinyurl"
  max_open_conns: 25
  max_idle_conns: 10

# Redis 配置
redis:
  host: "localhost"
  port: 6379
  password: "123123"
  db: 1
  pool_size: 10